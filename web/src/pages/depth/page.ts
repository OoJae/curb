// Depth page: STUB from lane A (foundation). The page owner replaces this file.
// Contract: call boot() first, then import this page's CSS, then enhance <main id="main">.
import { boot } from '../../shell/boot';
import './page.css';

boot({ page: 'depth' });
