{
  "name": "curb-ring-lab",
  "private": true,
  "type": "module",
  "description": "Week Ring dev harness (lane C). Not shipped. Needs `npm i` in web/ first (three and the site modules come from there). Run: npx vite web/lab/ring",
  "scripts": {
    "dev": "vite",
    "shots": "node scripts/shots.mjs",
    "poster": "node scripts/poster.mjs",
    "perf": "node scripts/perf.mjs",
    "probe": "node scripts/probe.mjs",
    "markup": "node scripts/home-markup.mjs",
    "review": "node scripts/review.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0",
    "sharp": "0.35.4",
    "vite": "8.3.1"
  }
}
