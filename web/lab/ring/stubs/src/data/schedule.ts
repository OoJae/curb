// LAB STUB for lane B's data/schedule.ts. Assumed: weekSlots(t) → { slots: Uint8Array(2016), nowIndex, regime, weekStart }.
import { stubWeek } from '../../../../../src/ring/layout';

export const weekSlots = (t: number) => stubWeek(t);
