// LAB STUB for lane B's data/api.ts. Assumed: preview402(path) → { url, status, accepts[], preview, at }.
// Exports nothing: the home page's own fallback makes the same unpaid call against api.curb.markets.
export {};
