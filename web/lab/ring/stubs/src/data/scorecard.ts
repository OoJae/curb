// LAB STUB for lane B's data/scorecard.ts. Assumed: readSkill() → { settled, beatLastPrint, beatClosingVwap, ties?, asOf }.
// Deliberately exports nothing: the home page then takes its spec'd fallback, the live /v1/accuracy-record preview.
export {};
