// LAB STUB for lane A's motion/reveal.ts (SplitText line reveals after fonts.ready, [data-reveal] entrances).
// Content is visible without it, so the stub does nothing.
export function reveal(_root: Element): void {}
